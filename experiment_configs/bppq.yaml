dataset:
    name: "bppq"
    path: "boilnserve/cabbage"
    subset: "agronomic_visual_cognition"
    split: "bppq"

model_input:
    question_type: multiple_choice
    pre_prompt: "You will be shown an image and asked a multiple-choice question about it."
    post_prompt: ""

process_results: 
    metric: option_match